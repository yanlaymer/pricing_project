# 🚀 Deployment Guide - Streamlit Community Cloud

## Руководство по развертыванию системы динамического ценообразования

---

## 📋 Предварительные требования

- [x] GitHub аккаунт
- [x] Streamlit Community Cloud аккаунт (бесплатно на share.streamlit.io)
- [x] Приложение с интегрированной авторизацией

---

## 🔐 Безопасность

### ⚠️ КРИТИЧЕСКИ ВАЖНО!

**Перед первым коммитом в GitHub:**

1. **.gitignore уже создан** ✅
   - Файл защищает `.streamlit/secrets.toml` от попадания в GitHub
   - Защищает базу данных и логи

2. **Измените пароль в `.streamlit/secrets.toml`**
   - Откройте `.streamlit/secrets.toml`
   - Замените `ChangeMeBeforeDeployment123!` на сильный пароль
   - Используйте минимум 12 символов (буквы, цифры, символы)

3. **Проверьте что secrets.toml НЕ отслеживается git:**
   ```bash
   git status
   # secrets.toml НЕ должен появляться в списке
   ```

---

## 🧪 Шаг 1: Локальное тестирование

### 1.1 Обновите credentials

Откройте `.streamlit/secrets.toml` и настройте:

```toml
[auth]
username = "admin"  # Ваш логин
password = "YourStrongPassword123!"  # ИЗМЕНИТЕ НА СИЛЬНЫЙ ПАРОЛЬ
```

### 1.2 Запустите приложение локально

```bash
cd /Users/ayannoorlan/Documents/sensata/pricing_project
streamlit run app.py
```

### 1.3 Протестируйте авторизацию

- ✅ Откроется форма логина
- ✅ Введите правильные credentials → доступ разрешен
- ✅ Введите неправильные credentials → отказ в доступе
- ✅ Выберите роль → проверьте навигацию
- ✅ Нажмите "Logout" → вернетесь к форме логина
- ✅ Проверьте все 4 роли работают

### 1.4 Если тесты не прошли

**Проблема: "KeyError: 'auth'"**
- Решение: Убедитесь что `.streamlit/secrets.toml` создан и содержит секцию `[auth]`

**Проблема: "No module named 'src.auth'"**
- Решение: Проверьте что `src/auth.py` существует

**Проблема: Форма логина не появляется**
- Решение: Проверьте что в `app.py` есть `if not check_password(): st.stop()`

---

## 📦 Шаг 2: Подготовка к GitHub

### 2.1 Инициализация Git (если еще не сделано)

```bash
cd /Users/ayannoorlan/Documents/sensata/pricing_project

# Проверка текущего статуса
git status

# Если git не инициализирован:
git init
git branch -M main
```

### 2.2 Создайте пустые файлы .gitkeep для сохранения структуры

```bash
touch data/raw/.gitkeep
touch data/processed/.gitkeep
touch data/exports/.gitkeep
```

### 2.3 Проверьте что secrets НЕ попадут в репозиторий

```bash
# Должны быть ИСКЛЮЧЕНЫ (не появляться):
git status | grep secrets.toml  # Не должно быть
git status | grep .db           # Не должно быть

# Должны быть ВКЛЮЧЕНЫ:
git status | grep .gitignore    # Должен быть
git status | grep app.py        # Должен быть
git status | grep src/auth.py   # Должен быть
```

### 2.4 Коммит изменений

```bash
git add .
git commit -m "Add authentication for Streamlit Cloud deployment"
```

---

## 🌐 Шаг 3: Создание репозитория на GitHub

### 3.1 Создайте репозиторий на GitHub

1. Перейдите на https://github.com
2. Нажмите "New repository"
3. Имя: `sensata-pricing-analytics` (или любое другое)
4. Описание: `Dynamic Pricing Analytics System for Real Estate`
5. Visibility: **Private** (рекомендуется для бизнес-приложений)
6. **НЕ добавляйте** README, .gitignore, license (уже есть локально)
7. Нажмите "Create repository"

### 3.2 Подключите локальный репозиторий к GitHub

```bash
# Скопируйте URL из GitHub (https://github.com/username/repo-name.git)

git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO_NAME.git
git push -u origin main
```

### 3.3 Проверьте GitHub

- Откройте репозиторий на GitHub
- ✅ Файлы должны быть загружены
- ⚠️ **Проверьте что `.streamlit/secrets.toml` НЕТ в репозитории!**
- ⚠️ **Проверьте что `data/*.db` НЕТ в репозитории!**

---

## ☁️ Шаг 4: Развертывание на Streamlit Community Cloud

### 4.1 Подключитесь к Streamlit Community Cloud

1. Перейдите на https://share.streamlit.io
2. Войдите через GitHub аккаунт
3. Разрешите Streamlit доступ к репозиториям

### 4.2 Создайте новое приложение

1. Нажмите **"New app"**
2. Выберите:
   - **Repository:** `your-username/sensata-pricing-analytics`
   - **Branch:** `main`
   - **Main file path:** `app.py`
   - **App URL:** `sensata-pricing` (или любой доступный)

### 4.3 Настройте Secrets (ВАЖНО!)

**Перед развертыванием:**

1. Нажмите **"Advanced settings..."** внизу
2. Найдите секцию **"Secrets"**
3. Скопируйте содержимое вашего локального `.streamlit/secrets.toml`:

```toml
[auth]
username = "admin"
password = "YourStrongPassword123!"
```

4. Вставьте в поле "Secrets"
5. **Убедитесь что пароль сильный и отличается от примера!**

### 4.4 Разверните приложение

1. Нажмите **"Deploy!"**
2. Дождитесь завершения (2-5 минут)
3. Статусы:
   - 🟡 "Building..." - установка зависимостей
   - 🟢 "Running" - приложение готово
   - 🔴 "Error" - см. логи для диагностики

---

## ✅ Шаг 5: Тестирование Production

### 5.1 Откройте deployed приложение

URL будет вида: `https://sensata-pricing.streamlit.app`

### 5.2 Чек-лист тестирования

- [ ] **Форма логина появляется**
- [ ] **Логин с правильными credentials работает**
- [ ] **Логин с неправильными credentials отклоняется**
- [ ] **Все 4 роли доступны**
- [ ] **Навигация работает**
- [ ] **Logout работает**
- [ ] **Данные импортируются** (если есть образцы)
- [ ] **Графики отображаются**
- [ ] **Производительность приемлема**

### 5.3 Известные ограничения Streamlit Community Cloud

⚠️ **База данных SQLite:**
- НЕ персистентна (рестарты сбросят данные)
- Каждый рестарт приложения = пустая БД
- Решения:
  - Вариант A: Каждый раз импортировать Excel заново
  - Вариант B: Использовать внешнюю БД (PostgreSQL, MySQL)
  - Вариант C: Использовать облачное хранилище (S3, GCS)

⚠️ **Ресурсы:**
- CPU: 1 vCPU
- RAM: 1 GB
- Хранилище: Временное (не персистентное)
- Ограничение трафика: разумное использование

⚠️ **Автоматические рестарты:**
- После 7 дней неактивности
- При обновлении кода (git push)
- При изменении dependencies

---

## 🔧 Шаг 6: Обновление приложения

### 6.1 Изменение кода

```bash
# Внесите изменения в код
git add .
git commit -m "Update: description of changes"
git push origin main
```

**Результат:** Streamlit Community Cloud автоматически перезапустит приложение (2-5 минут)

### 6.2 Изменение Secrets (пароля)

1. Перейдите на https://share.streamlit.io
2. Откройте ваше приложение
3. Нажмите "⋮" → "Settings" → "Secrets"
4. Обновите credentials
5. Нажмите "Save"
6. Приложение автоматически перезапустится

**Важно:** Изменения secrets не требуют git push!

### 6.3 Изменение dependencies

Отредактируйте `requirements.txt`:

```bash
# Добавьте/обновите библиотеки
git add requirements.txt
git commit -m "Update dependencies"
git push origin main
```

---

## 🐛 Устранение неполадок

### Проблема: "Secrets configuration error"

**Симптом:** Ошибка при логине
**Решение:**
1. Проверьте что secrets настроены в Streamlit Cloud
2. Формат должен быть точно `[auth]` с username и password
3. Перезапустите приложение

### Проблема: "Module not found"

**Симптом:** Import errors при запуске
**Решение:**
1. Проверьте `requirements.txt` содержит все зависимости
2. Проверьте версии библиотек
3. Проверьте логи в Streamlit Cloud

### Проблема: База данных пустая после рестарта

**Симптом:** Нет данных после рестарта
**Решение:**
1. Это нормально для SQLite на Community Cloud
2. Импортируйте Excel заново
3. Или настройте внешнюю БД для персистентности

### Проблема: Приложение медленное

**Симптом:** Долгая загрузка страниц
**Решение:**
1. Оптимизируйте запросы к БД
2. Используйте `@st.cache_data` для кэширования
3. Уменьшите объем данных
4. Рассмотрите Streamlit Teams/Enterprise (платно) для больших ресурсов

### Проблема: "App exceeded resource limits"

**Симптом:** Приложение крашится
**Решение:**
1. Уменьшите объем загружаемых данных
2. Оптимизируйте memory usage
3. Используйте пагинацию для больших таблиц
4. Рассмотрите платный tier

---

## 🔒 Лучшие практики безопасности

### Управление паролями

✅ **Делайте:**
- Используйте уникальные сильные пароли (12+ символов)
- Храните пароли в password manager
- Меняйте пароли периодически (раз в 3-6 месяцев)
- Используйте разные пароли для dev/staging/production

❌ **Не делайте:**
- Не коммитьте пароли в GitHub
- Не используйте простые пароли ("password", "admin123")
- Не храните пароли в незащищенных файлах
- Не делитесь паролями через email/мессенджеры

### Аудит доступа

Периодически проверяйте:
- Кто имеет доступ к GitHub репозиторию
- Кто имеет доступ к Streamlit Cloud app
- Логи входов (если настроены)

### Обновления

- Обновляйте Streamlit и зависимости регулярно
- Следите за security advisories
- Тестируйте обновления локально перед deployment

---

## 📊 Мониторинг приложения

### Встроенные метрики Streamlit Cloud

1. Откройте приложение на share.streamlit.io
2. Нажмите "⋮" → "Analytics"
3. Доступно:
   - Количество просмотров
   - Активные пользователи
   - География пользователей
   - Время загрузки

### Логи

1. Откройте приложение на share.streamlit.io
2. Нажмите "⋮" → "Logs"
3. Смотрите:
   - Application logs (print, st.write)
   - Errors и stack traces
   - Startup logs

---

## 🚀 Следующие шаги (опционально)

### Улучшение персистентности данных

**Вариант 1: PostgreSQL на Supabase (бесплатно)**
1. Создайте аккаунт на supabase.com
2. Создайте проект
3. Обновите `src/database/models.py` для PostgreSQL
4. Добавьте connection string в secrets

**Вариант 2: Google Cloud SQL**
- Платно, но enterprise-grade
- Автоматические бэкапы
- High availability

**Вариант 3: MongoDB Atlas (бесплатный tier)**
- NoSQL подход
- 512 MB бесплатно
- Geo-распределение

### Добавление advanced features

- Multi-user support (несколько аккаунтов)
- Role-based access control (RBAC)
- Audit logging (кто, что, когда)
- 2FA authentication
- Session timeout
- Rate limiting

### Масштабирование

- **Streamlit Teams** ($250/month):
  - 3 editors
  - Unlimited viewers
  - 4 vCPU, 16 GB RAM
  - Private apps
  - SSO support

- **Streamlit Enterprise:**
  - Custom pricing
  - On-premise deployment
  - Dedicated support
  - SLA guarantees

---

## 📞 Поддержка

### Документация

- Streamlit: https://docs.streamlit.io
- Streamlit Community Cloud: https://docs.streamlit.io/streamlit-community-cloud
- Secrets management: https://docs.streamlit.io/streamlit-community-cloud/deploy-your-app/secrets-management

### Community

- Streamlit Forum: https://discuss.streamlit.io
- GitHub Issues: https://github.com/streamlit/streamlit/issues
- Discord: https://discord.gg/streamlit

### Внутренняя поддержка

- Технические вопросы: [your-tech-email]
- Доступ к приложению: [your-admin-email]
- Смена паролей: [your-security-email]

---

## ✅ Чек-лист развертывания

Отметьте каждый пункт:

- [ ] .gitignore создан
- [ ] Пароль изменен в .streamlit/secrets.toml
- [ ] Локальное тестирование пройдено
- [ ] secrets.toml НЕ попал в GitHub
- [ ] Код загружен в GitHub
- [ ] Streamlit Cloud аккаунт создан
- [ ] Репозиторий подключен к Streamlit Cloud
- [ ] Secrets настроены в Streamlit Cloud UI
- [ ] Приложение развернуто успешно
- [ ] Production тестирование пройдено
- [ ] URL приложения сохранен
- [ ] Пароль сохранен в password manager
- [ ] Команда проинформирована о deployment

---

**Готово к использованию!** 🎉

Ваше приложение Dynamic Pricing Analytics теперь доступно онлайн с защитой паролем.

**URL:** `https://your-app-name.streamlit.app`
**Login:** Из secrets.toml
**Password:** Из secrets.toml

---

**Последнее обновление:** 17 ноября 2025
**Версия:** 1.0.0
